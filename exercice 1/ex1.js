
  var min1 = Math.min(100, 2, 25, 36, 57,3);
  console.log(min1);
   for (let i = 0; i<tab.length; i++) {
   if (tab[i] == min1) {
    console.log(tab.slice(i,1));
  }
   }
   var tab = [100, 2, 25, 36, 57,3];
   const result = tabs.filter(tab => Math.min(tab));

   
   
